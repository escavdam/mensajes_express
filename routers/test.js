//const { getAll, add, reset } = require('/scripts/mensajes.js');
////const { getAll, add, reset } = require('./scripts/mensajes.js');
const { getAll, add, reset } = require('../scripts/mensajes.js');

console.log(getAll());
